enum ERRORS {
  ERROR_DEFAULT = 'Oops, something went wrong',
}

enum STATUS {
  DEFAULT = 'DEFAULT',
  PASSING = 'PASSING',
  PARTIAL = 'PARTIAL',
  FAILING = 'FAILING',
}

export { STATUS, ERRORS };
